<% if @item.valid? %>
  $('#product-<%= @product.id %> .quantity').text(<%= @item.quantity %>)
  $(".cart-items-count").text(<%= current_user.current_cart.items.count %>);
  $('#toast-container').hide()
  toastr.success("Removido produto <%= @product.name %>!");
<% end %>
